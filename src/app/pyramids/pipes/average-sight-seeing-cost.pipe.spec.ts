import { AverageSightSeeingCostPipe } from './average-sight-seeing-cost.pipe';

describe('AverageSightSeeingCostPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageSightSeeingCostPipe();
    expect(pipe).toBeTruthy();
  });
});
