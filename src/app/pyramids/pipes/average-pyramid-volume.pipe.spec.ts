import { AveragePyramidVolumePipe } from './average-pyramid-volume.pipe';

describe('AveragePyramidVolumePipe', () => {
	it('create an instance', () => {
		const pipe = new AveragePyramidVolumePipe();
		expect(pipe).toBeTruthy();
	});
});
