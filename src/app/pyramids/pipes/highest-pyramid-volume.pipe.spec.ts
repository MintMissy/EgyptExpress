import { HighestPyramidVolumePipe } from './highest-pyramid-volume.pipe';

describe('HighestPyramidVolumePipe', () => {
	it('create an instance', () => {
		const pipe = new HighestPyramidVolumePipe();
		expect(pipe).toBeTruthy();
	});
});
