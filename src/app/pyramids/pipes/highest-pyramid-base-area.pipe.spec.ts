import { HighestPyramidBaseAreaPipe } from './highest-pyramid-base-area.pipe';

describe('HighestPyramidBaseAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new HighestPyramidBaseAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
